<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./../includes/head.ejs'); %>

    <link rel="stylesheet" href="/css/forms.css" />
  </head>

  <body>
    <main class="main">
      <div class="main__container">
        <form 
          action="/auth/create-account"
          class="form form--small"
          method="POST"
        >
          <%- 
            include('./../includes/title.ejs', { backLink: '/home/settings' });
          %>

          <ul class="form__list">
            <li class="form__item">
              <label for="old-password" class="form__label">
                <span>Old Password <span class="form__required">*</span></span>
              </label>
              <input
                type="password"
                class="form__input"
                name="old-password"
                required
              />
            </li>

            <li class="form__item">
              <label for="new-password" class="form__label">
                <span>New Password <span class="form__required">*</span></span>
              </label>
              <input
                type="password"
                class="form__input"
                name="new-password"
                required
              />
            </li>

            <li class="form__item">
              <label for="confirm-new-password" class="form__label">
                <span>
                  Confirm New Password <span class="form__required">*</span>
                </span>
              </label>
              <input
                type="password"
                class="form__input"
                name="confirm-new-password"
                required
              />
            </li>
          </ul>

          <%- include('./../includes/buttonRow.ejs', { 
            cancelLink: '/home/settings',
            disabled: false 
          }); %>
        </form>
      </div>
    </main>
  </body>
</html>
